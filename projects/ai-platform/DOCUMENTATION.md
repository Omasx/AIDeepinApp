# 📚 توثيق منصة AI DePIN Cloud

وثائق شاملة لمنصة الذكاء الاصطناعي السحابية اللامركزية

## 📖 جدول المحتويات

1. [المقدمة](#المقدمة)
2. [البنية المعمارية](#البنية-المعمارية)
3. [المكونات الأساسية](#المكونات-الأساسية)
4. [API الكامل](#api-الكامل)
5. [أمثلة الاستخدام](#أمثلة-الاستخدام)
6. [الإعدادات المتقدمة](#الإعدادات-المتقدمة)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

## المقدمة

منصة AI DePIN Cloud هي نظام متكامل يجمع بين:

- **الذكاء الاصطناعي**: تنفيذ الأوامر الطبيعية وتوليد الأكواد
- **تشغيل الألعاب**: تشغيل وتحكم تلقائي بالألعاب
- **التخزين الكمي**: ضغط وتوزيع البيانات بكفاءة
- **المجانية الكاملة**: بدون رسوم أبداً

## البنية المعمارية

```
┌─────────────────────────────────────────────────┐
│         Frontend (HTML/CSS/JavaScript)          │
│  - واجهة مستخدم حديثة                           │
│  - دردشة مع AI                                  │
│  - ترمينال مدمج                                │
└────────────────┬────────────────────────────────┘
                 │ HTTP/WebSocket
┌────────────────▼────────────────────────────────┐
│      Backend (aiohttp Server)                   │
│  - معالجة الطلبات                              │
│  - إدارة الجلسات                               │
│  - تنسيق المهام                                │
└────────────────┬────────────────────────────────┘
                 │
    ┌────────────┼────────────┬──────────────┐
    │            │            │              │
┌───▼──┐  ┌─────▼─────┐  ┌──▼──────┐  ┌──▼──────┐
│ AI   │  │ Game      │  │ Storage │  │ Session │
│Agent │  │Controller │  │ Manager │  │ Manager │
└──────┘  └───────────┘  └─────────┘  └─────────┘
```

## المكونات الأساسية

### 1. الواجهة الأمامية (Frontend)

**الملف**: `frontend/index.html`

الواجهة توفر:
- تسجيل دخول مجاني
- لوحة تحكم شاملة
- دردشة مع الوكيل الذكي
- ترمينال سحابي
- إدارة مفاتيح API

**الصفحات الرئيسية**:
- Dashboard: لوحة التحكم الرئيسية
- AI Agent: الوكيل الذكي والدردشة
- Terminal: الترمينال السحابي
- Settings: الإعدادات

### 2. السيرفر الرئيسي (Backend)

**الملف**: `backend/server.py`

السيرفر يعالج:
- الاتصالات الجديدة
- مزامنة مفاتيح API
- تنفيذ الأوامر
- إدارة WebSocket
- الإحصائيات

**المنافذ**:
- HTTP: 8080 (افتراضي)
- WebSocket: 8080/ws

### 3. الوكيل الذكي (AI Agent)

**الملف**: `backend/ai_agent.py`

الوكيل يدعم:
- تنفيذ الأوامر الطبيعية
- قراءة الشاشة
- توليد الأكواد
- بناء المواقع
- محادثات AI

**أنواع المهام**:
- GAME: تشغيل الألعاب
- AI_CHAT: محادثات AI
- SCREENSHOT: أخذ لقطات
- CODE_GENERATION: توليد أكواد
- WEBSITE_BUILDER: بناء مواقع

### 4. متحكم الألعاب (Game Controller)

**الملف**: `backend/game_controller.py`

المتحكم يوفر:
- تشغيل الألعاب
- التحكم التلقائي
- تسجيل الفيديو
- إحصائيات الأداء
- إدارة الجلسات

**الألعاب المدعومة**:
- Fortnite
- PUBG
- Valorant
- League of Legends
- وغيرها...

### 5. التخزين الكمي (Quantum Storage)

**الملف**: `backend/quantum_storage.py`

التخزين يوفر:
- ضغط LZMA (70% توفير)
- إزالة التكرار
- ذاكرة مؤقتة LRU
- توزيع P2P
- مجاني تماماً

**الميزات**:
- حجم ذاكرة: 2GB (قابل للتخصيص)
- معدل الضغط: 0.3 (70% توفير)
- سرعة الوصول: < 50ms

### 6. مدير الجلسات (Session Manager)

**الملف**: `backend/session_manager.py`

المدير يدير:
- إنشاء جلسات مجانية
- التحقق من الصلاحية
- تحديث الإحصائيات
- إدارة الجلسات المنتهية

**مدة الجلسة**: 30 يوم (مجاني)

## API الكامل

### 1. الاتصال والمصادقة

#### POST /connect
إنشاء جلسة جديدة

```json
Request:
{
    "device_id": "device_12345"
}

Response:
{
    "success": true,
    "session_token": "abc123def456...",
    "created_at": 1707300000,
    "expires_at": 1709892000,
    "cost": "0 USD",
    "message": "مرحباً! اتصالك مجاني 100%"
}
```

### 2. تنفيذ الأوامر

#### POST /api/agent/execute
تنفيذ أمر عبر الوكيل الذكي

```json
Request:
Headers: Authorization: Bearer {token}
{
    "command": "افتح Fortnite والعب 5 مباريات"
}

Response:
{
    "success": true,
    "task_id": "task_1707300000000",
    "message": "جاري التنفيذ...",
    "response": "تم تنفيذ الأمر بنجاح"
}
```

#### POST /api/terminal/execute
تنفيذ أمر في الترمينال

```json
Request:
Headers: Authorization: Bearer {token}
{
    "command": "ls -la"
}

Response:
{
    "success": true,
    "output": "total 48\ndrwxr-xr-x...",
    "error": null
}
```

### 3. مزامنة المفاتيح

#### POST /api/sync-keys
مزامنة مفاتيح API

```json
Request:
Headers: Authorization: Bearer {token}
{
    "openai": "sk-...",
    "anthropic": "sk-ant-...",
    "google": "AIza...",
    "deepseek": "sk-..."
}

Response:
{
    "success": true,
    "message": "تمت المزامنة بنجاح!",
    "hash": "abc123..."
}
```

### 4. الإحصائيات

#### GET /stats
الحصول على إحصائيات النظام

```json
Response:
{
    "server_type": "AI DePIN Cloud Platform",
    "status": "🟢 متصل",
    "storage": {
        "cache_size_mb": 512,
        "max_size_mb": 2048,
        "items_count": 150,
        "hit_rate": 85.5,
        "cost": "0 USD"
    },
    "sessions": {
        "active_sessions": 10,
        "total_ai_requests": 250,
        "total_games_played": 50,
        "total_apps_created": 20,
        "total_data_used_mb": 1024,
        "total_cost": "0 USD"
    },
    "timestamp": "2026-02-07T10:30:00"
}
```

#### GET /health
فحص صحة السيرفر

```json
Response:
{
    "status": "healthy",
    "timestamp": "2026-02-07T10:30:00",
    "uptime": "running"
}
```

### 5. WebSocket

#### ws://localhost:8080/ws
اتصال WebSocket للتحديثات الفورية

```json
Message Types:
{
    "type": "agent_response",
    "message": "رد من الوكيل الذكي"
}

{
    "type": "notification",
    "message": "إشعار",
    "level": "info"
}

{
    "type": "task_update",
    "task": {...}
}
```

## أمثلة الاستخدام

### مثال 1: تشغيل لعبة

```python
import requests

# الاتصال
response = requests.post('http://localhost:8080/connect', json={
    'device_id': 'mobile_12345'
})
token = response.json()['session_token']

# تشغيل اللعبة
response = requests.post(
    'http://localhost:8080/api/agent/execute',
    headers={'Authorization': f'Bearer {token}'},
    json={'command': 'افتح Fortnite'}
)

print(response.json())
```

### مثال 2: توليد كود

```python
# توليد كود Python
response = requests.post(
    'http://localhost:8080/api/agent/execute',
    headers={'Authorization': f'Bearer {token}'},
    json={'command': 'اكتب لي تطبيق todo list بـ Python'}
)

code = response.json()['result']['code']
print(code)
```

### مثال 3: استخدام الترمينال

```python
# تنفيذ أمر
response = requests.post(
    'http://localhost:8080/api/terminal/execute',
    headers={'Authorization': f'Bearer {token}'},
    json={'command': 'python3 --version'}
)

output = response.json()['output']
print(output)
```

## الإعدادات المتقدمة

### ملف .env

```bash
# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
DEBUG=True

# Storage
STORAGE_PATH=./sessions
CACHE_SIZE_MB=2048

# AI Models
OPENAI_API_KEY=your_key_here
ANTHROPIC_API_KEY=your_key_here
GOOGLE_API_KEY=your_key_here
DEEPSEEK_API_KEY=your_key_here

# Game Settings
DEFAULT_FPS=60
DEFAULT_RESOLUTION=1920x1080
DEFAULT_GRAPHICS=ultra

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/platform.log
```

### تخصيص الإعدادات

```python
# في backend/server.py
server = AIDePINServer()
server.run(
    host="0.0.0.0",
    port=9000  # منفذ مخصص
)

# في backend/quantum_storage.py
storage = QuantumFreeStorage(
    cache_size_mb=4096  # حجم مخصص
)
```

## استكشاف الأخطاء

### المشكلة: خطأ "Address already in use"

```bash
# ابحث عن العملية المستخدمة للمنفذ
lsof -i :8080

# اقتل العملية
kill -9 <PID>

# أو استخدم منفذ مختلف
python3 server.py --port 9000
```

### المشكلة: خطأ في استيراد المكاتب

```bash
# تحقق من البيئة الافتراضية
which python3

# أعد تثبيت المكاتب
pip install -r backend/requirements.txt --force-reinstall

# تحقق من الإصدارات
pip list
```

### المشكلة: الواجهة لا تحمل

```bash
# افتح أدوات المطور (F12)
# تحقق من Network tab
# تحقق من Console tab للأخطاء

# تحقق من CORS
# تأكد من أن السيرفر يسمح بـ CORS
```

### المشكلة: الأوامر لا تُنفذ

```bash
# تحقق من السجلات
tail -f logs/platform.log

# تحقق من صحة الأمر
# استخدم أوامر بسيطة للاختبار

# تحقق من الجلسة
# تأكد من أن الجلسة صالحة
```

## الأداء والتحسين

### نصائح الأداء

1. **التخزين**: استخدم حجم ذاكرة مناسب
2. **الضغط**: LZMA يوفر 70% من المساحة
3. **الشبكة**: استخدم WebSocket للتحديثات الفورية
4. **الجلسات**: امسح الجلسات المنتهية دورياً

### مراقبة الأداء

```bash
# مراقبة استخدام الموارد
watch -n 1 'ps aux | grep python3'

# مراقبة المنفذ
netstat -an | grep 8080

# مراقبة السجلات
tail -f logs/platform.log
```

## الأمان

### أفضل الممارسات

1. استخدم HTTPS في الإنتاج
2. قم بتشفير المفاتيح الحساسة
3. استخدم جدران الحماية
4. راقب السجلات بانتظام
5. حدّث المكاتب بانتظام

### تشفير الجلسات

```python
import hashlib

# إنشاء جلسة مشفرة
session_data = f"{device_id}:{timestamp}:{random}".encode()
session_token = hashlib.sha256(session_data).hexdigest()
```

## الخلاصة

منصة AI DePIN Cloud توفر:
- ✅ واجهة احترافية
- ✅ سيرفر قوي
- ✅ وكيل ذكي
- ✅ تخزين فعال
- ✅ مجاني 100%

للمزيد من المعلومات، راجع README.md أو اتصل بالدعم.

---

**آخر تحديث**: 2026-02-07  
**الإصدار**: 1.0.0
