# 📲 دليل التثبيت الكامل

## 🎯 نظرة عامة

هذا الدليل يشرح كيفية تثبيت تطبيق **AI Agent DePIN** على هاتفك الذكي.

---

## 📋 المتطلبات

### على الهاتف
- **نظام التشغيل**: Android 7.0 أو أعلى (API 24+)
- **مساحة التخزين**: 20GB على الأقل (مع Llama 3.5)
- **ذاكرة RAM**: 6GB على الأقل (8GB موصى به)
- **معالج**: Snapdragon 888+ أو أعلى
- **الاتصال**: WiFi أو 4G/5G

### على جهاز الكمبيوتر (للبناء)
- **Android Studio** 2023.1 أو أعلى
- **JDK** 11 أو أعلى
- **Gradle** 8.0 أو أعلى
- **Python** 3.8+ (اختياري)

---

## 🚀 طريقة التثبيت السريعة

### الطريقة 1: تثبيت مباشر (الأسهل)

#### الخطوة 1: تحميل ملف APK
```
1. اذهب إلى: https://releases.aidepin.app/app-release.apk
2. اضغط تحميل
3. انتظر انتهاء التحميل
```

#### الخطوة 2: تثبيت التطبيق
```
1. افتح تطبيق "الملفات" على هاتفك
2. اذهب إلى مجلد "التحميلات"
3. ابحث عن "app-release.apk"
4. اضغط عليه
5. اضغط "تثبيت"
6. انتظر انتهاء التثبيت
```

#### الخطوة 3: تشغيل التطبيق
```
1. اضغط "فتح" أو
2. ابحث عن "AI Agent DePIN" في تطبيقاتك
3. اضغط عليه لتشغيله
```

---

### الطريقة 2: تثبيت عبر ADB (للمطورين)

#### الخطوة 1: تثبيت أدوات التطوير
```bash
# على Mac
brew install android-platform-tools

# على Linux
sudo apt-get install android-tools-adb

# على Windows
# حمل من: https://developer.android.com/studio/releases/platform-tools
```

#### الخطوة 2: توصيل الهاتف
```bash
# تفعيل وضع المطور على الهاتف
# الإعدادات > حول الهاتف > رقم الإصدار (اضغط 7 مرات)

# تفعيل تصحيح USB
# الإعدادات > خيارات المطور > تصحيح USB

# التحقق من الاتصال
adb devices
# يجب أن ترى جهازك في القائمة
```

#### الخطوة 3: تثبيت APK
```bash
# نقل إلى مجلد APK
cd /path/to/apk/folder

# تثبيت التطبيق
adb install app-release.apk

# أو إعادة تثبيت
adb install -r app-release.apk
```

#### الخطوة 4: تشغيل التطبيق
```bash
adb shell am start -n com.aidepin.app/.MainActivity
```

---

### الطريقة 3: البناء من المصدر

#### الخطوة 1: استخراج الملف المضغوط
```bash
unzip AIDeepinApp-Final-Complete.zip
cd AIDeepinApp-Final-Complete
```

#### الخطوة 2: فتح في Android Studio
```bash
# أو افتح المجلد مباشرة في Android Studio
# File > Open > اختر المجلد
```

#### الخطوة 3: بناء التطبيق
```bash
# في Android Studio:
# Build > Build Bundle(s) / APK(s) > Build APK(s)

# أو عبر سطر الأوامر:
./gradlew assembleRelease
```

#### الخطوة 4: تثبيت APK
```bash
# سيكون الملف في:
# app/build/outputs/apk/release/app-release.apk

# ثم اتبع الطريقة 1 أو 2
```

---

## 🔧 الإعدادات الأولية

### بعد التثبيت مباشرة

#### 1. السماح بالأذونات
عند فتح التطبيق لأول مرة، سيطلب منك:
- ✅ الوصول إلى الملفات
- ✅ الوصول إلى الكاميرا
- ✅ الوصول إلى الميكروفون
- ✅ الوصول إلى الموقع

**اضغط "السماح" لجميع الأذونات**

#### 2. إعدادات Llama 3.5
```
1. اذهب إلى Settings > AI Models
2. اختر Llama 3.5
3. اضغط Download
4. انتظر انتهاء التحميل (قد يستغرق وقتاً طويلاً)
```

#### 3. الاتصال بالسيرفر
```
1. اذهب إلى Settings > Server
2. أدخل عنوان السيرفر (مثلاً: https://api.aidepin.app)
3. اضغط Connect
```

---

## 🧪 الاختبار الأول

### تجربة الواجهة الرئيسية
```
1. افتح التطبيق
2. يجب أن ترى:
   - عرض الموارد (CPU, NET, Storage, Power)
   - 4 أزرار رئيسية (CODE, DATA, TEST, DEPLOY)
   - زر AI AGENT في الأسفل
```

### تجربة Llama 3.5
```
1. اضغط على أيقونة الدردشة
2. اكتب رسالة (مثلاً: "مرحبا")
3. اضغط إرسال
4. يجب أن ترى استجابة من Llama
```

### تجربة الوكيل الذكي
```
1. اضغط على زر AI AGENT
2. أعط أمراً (مثلاً: "أنشئ تطبيق")
3. يجب أن يبدأ الوكيل بالعمل
```

---

## 🆘 حل المشاكل الشائعة

### المشكلة: "لا يمكن تثبيت التطبيق"
**الحل:**
```
1. تأكد من أن لديك مساحة تخزين كافية
2. احذف التطبيق السابق إن وجد
3. حاول مرة أخرى
```

### المشكلة: "التطبيق لا يفتح"
**الحل:**
```
1. أعد تشغيل الهاتف
2. احذف بيانات التطبيق:
   - الإعدادات > التطبيقات > AI Agent DePIN > حذف البيانات
3. أعد تثبيت التطبيق
```

### المشكلة: "Llama لا يعمل"
**الحل:**
```
1. تأكد من تحميل النموذج:
   - Settings > AI Models > Llama 3.5
2. تحقق من الاتصال بالإنترنت
3. أعد تحميل النموذج
```

### المشكلة: "استهلاك عالي للبطارية"
**الحل:**
```
1. قلل جودة الرسومات:
   - Settings > Graphics > Quality
2. أغلق التطبيقات الأخرى
3. استخدم وضع توفير الطاقة
```

### المشكلة: "بطء في الأداء"
**الحل:**
```
1. أغلق التطبيقات الأخرى
2. امسح ذاكرة التطبيق:
   - الإعدادات > التطبيقات > AI Agent DePIN > حذف البيانات
3. أعد تشغيل الهاتف
```

---

## 📊 المتطلبات الدقيقة

| المتطلب | الحد الأدنى | الموصى به |
|--------|-----------|---------|
| Android | 7.0 (API 24) | 12.0+ (API 31+) |
| RAM | 6GB | 8GB+ |
| Storage | 20GB | 50GB+ |
| CPU | Snapdragon 888 | Snapdragon 8 Gen 2+ |
| GPU | Adreno 660 | Adreno 8 Gen 2+ |
| Display | 1080p | 1440p+ |

---

## 🔒 الأمان

### نصائح الأمان
✅ حمّل التطبيق من مصدر موثوق فقط  
✅ تحقق من توقيع التطبيق  
✅ استخدم كلمة مرور قوية  
✅ فعّل المصادقة الثنائية  
✅ لا تشارك بيانات حساسة  

### الأذونات المطلوبة
- **الملفات**: لقراءة وكتابة البيانات
- **الكاميرا**: لالتقاط الفيديو
- **الميكروفون**: للتسجيل الصوتي
- **الموقع**: للخدمات المحلية
- **الإنترنت**: للاتصال بالسيرفر

---

## 📞 الدعم

إذا واجهت مشكلة:
1. تحقق من هذا الدليل
2. اقرأ الأسئلة الشائعة
3. اتصل بالدعم الفني

**البريد الإلكتروني**: support@aidepin.app  
**الموقع**: https://aidepin.app  
**Discord**: https://discord.gg/aidepin  

---

## ✅ قائمة التحقق

قبل الاستخدام، تأكد من:
- [ ] تثبيت التطبيق بنجاح
- [ ] السماح بجميع الأذونات
- [ ] تحميل نموذج Llama 3.5
- [ ] الاتصال بالسيرفر
- [ ] اختبار الواجهة الرئيسية
- [ ] اختبار Llama 3.5
- [ ] اختبار الوكيل الذكي

---

**آخر تحديث**: 2026-02-07  
**الإصدار**: 1.0.0
