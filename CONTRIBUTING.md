# 🤝 دليل المساهمة

شكراً لاهتمامك بالمساهمة في مشروع **AI Agent DePIN**! 

---

## 📋 قواعد المساهمة

### 1. Fork المستودع
```bash
# انسخ المستودع إلى حسابك
git clone https://github.com/YOUR_USERNAME/AIDeepinApp.git
cd AIDeepinApp
```

### 2. إنشاء فرع جديد
```bash
# أنشئ فرع لميزتك الجديدة
git checkout -b feature/your-feature-name
# أو للإصلاحات
git checkout -b fix/bug-description
```

### 3. إجراء التغييرات
```bash
# عدّل الملفات
# اختبر التغييرات
# تأكد من عدم وجود أخطاء
```

### 4. Commit التغييرات
```bash
# أضف الملفات
git add .

# اكتب رسالة واضحة
git commit -m "feat: add new feature" 
# أو
git commit -m "fix: resolve issue #123"
```

### 5. Push إلى GitHub
```bash
git push origin feature/your-feature-name
```

### 6. إنشاء Pull Request
- اذهب إلى المستودع الأصلي
- اضغط "New Pull Request"
- اختر فرعك
- اكتب وصف واضح
- اضغط "Create Pull Request"

---

## 📝 معايير الكود

### Kotlin/Java
```kotlin
// استخدم camelCase للمتغيرات والدوال
val userName = "John"
fun getUserName(): String { }

// استخدم PascalCase للفئات
class UserManager { }

// أضف تعليقات واضحة
/**
 * وصف الدالة
 * @param param1 وصف المعامل
 * @return وصف القيمة المرجعة
 */
fun myFunction(param1: String): String { }
```

### XML/Layout
```xml
<!-- استخدم أسماء واضحة -->
<Button
    android:id="@+id/btn_submit"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/submit" />
```

### Python
```python
# اتبع PEP 8
def my_function(param1, param2):
    """وصف الدالة"""
    return param1 + param2

# استخدم type hints
def process_data(data: List[str]) -> Dict[str, int]:
    pass
```

---

## 🧪 الاختبار

قبل إرسال Pull Request:

```bash
# اختبر الكود
./gradlew test

# تحقق من الأخطاء
./gradlew lint

# بناء التطبيق
./gradlew assembleRelease
```

---

## 📦 أنواع الـ Commits

استخدم هذه البادئات:

| البادئة | الوصف | مثال |
|--------|-------|------|
| `feat:` | ميزة جديدة | `feat: add Llama integration` |
| `fix:` | إصلاح خطأ | `fix: resolve memory leak` |
| `docs:` | تحديث التوثيق | `docs: update README` |
| `style:` | تنسيق الكود | `style: format code` |
| `refactor:` | إعادة هيكلة | `refactor: optimize algorithm` |
| `perf:` | تحسين الأداء | `perf: improve startup time` |
| `test:` | إضافة اختبارات | `test: add unit tests` |
| `chore:` | مهام صيانة | `chore: update dependencies` |

---

## 🐛 الإبلاغ عن الأخطاء

### قبل الإبلاغ
- تحقق من Issues الموجودة
- جرب النسخة الأحدث
- اجمع معلومات مفيدة

### عند الإبلاغ
```markdown
## الوصف
وصف واضح للمشكلة

## خطوات التكرار
1. افتح التطبيق
2. اضغط على الزر
3. لاحظ الخطأ

## السلوك المتوقع
ماذا كان يجب أن يحدث

## السلوك الفعلي
ماذا حدث بالفعل

## معلومات النظام
- Android: 12
- الهاتف: Samsung Galaxy S21
- الإصدار: 1.0.0
```

---

## 💡 اقتراح ميزات جديدة

### قبل الاقتراح
- تحقق من الميزات الموجودة
- ابحث في Issues

### عند الاقتراح
```markdown
## الوصف
وصف الميزة المطلوبة

## الفائدة
لماذا هذه الميزة مهمة

## التنفيذ المقترح
كيف يمكن تنفيذها

## بدائل أخرى
هل هناك حلول أخرى
```

---

## 📚 الموارد المفيدة

- [توثيق Android](https://developer.android.com)
- [Kotlin Documentation](https://kotlinlang.org/docs)
- [Git Guide](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com)

---

## ✅ قائمة التحقق قبل الإرسال

- [ ] الكود يعمل بدون أخطاء
- [ ] الاختبارات تمر بنجاح
- [ ] لا توجد تحذيرات Lint
- [ ] التوثيق محدث
- [ ] الرسالة واضحة
- [ ] لا توجد ملفات غير ضرورية

---

## 🎯 معايير القبول

سيتم قبول Pull Request إذا:
- ✅ الكود يتبع معايير المشروع
- ✅ الاختبارات تمر بنجاح
- ✅ التوثيق محدث
- ✅ لا توجد conflicts
- ✅ الأداء لم يتأثر سلباً

---

## 📞 التواصل

- **Issues**: للأخطاء والاقتراحات
- **Discussions**: للنقاشات العامة
- **Email**: support@aidepin.app
- **Discord**: https://discord.gg/aidepin

---

شكراً لمساهمتك! 🙏

**آخر تحديث**: 2026-02-07
